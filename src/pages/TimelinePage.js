import styled from "styled-components"

export default function TimelinePage(){
    return (
        <Background>

        </Background>
    )
}
const Background = styled.div`
    margin-top: 72px;
    background: #333;
    height: 100vh;
`